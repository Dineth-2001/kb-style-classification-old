version: '3.8'

services:
  backend-api:
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn server:app --host 0.0.0.0 --port 5000
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    env_file:
      - .env
