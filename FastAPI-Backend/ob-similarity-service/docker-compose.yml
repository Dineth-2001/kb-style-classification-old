version: '3.8'

services:
  ob-api:
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn server:app --host 0.0.0.0 --port 8001
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    env_file:
      - .env
